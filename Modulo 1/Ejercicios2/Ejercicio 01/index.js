/**
 * Crea una función que reciba un número y devuelva el doble de ese número.
 * @param {number} numero 
 * @returns number
 */ 
function doble(numero) {
    return numero * 2;
};

console.log(doble(3));